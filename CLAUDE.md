# CLAUDE.md

**ALWAYS read [`AGENTS.md`](AGENTS.md) before doing anything — regardless of
how simple the task seems.** No exceptions: a one-line edit, a question, a
lookup — read it first. It is the single entry point; it routes to
[`docs/`](docs/README.md) where all product, architecture, porting, and
decision knowledge lives.

Do not duplicate guidance here. `AGENTS.md` is the source of truth so every
coding agent (Claude Code, Codex, or any other) shares one contract.
